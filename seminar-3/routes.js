// Global
const HOME = "/";

// Users
const USERS = "/users"
const SIGNUP = "/signup";
const SIGNIN = "/signin";


const routes = {
    home: HOME,
    users: USERS,
    signup: SIGNUP,
    signin: SIGNIN
}

module.exports = routes;